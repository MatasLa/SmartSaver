namespace ePiggy.utilities
{
    public enum EntryType
    {
        Income,
        Expense
    }
    public enum Importance
    {
        Necessary = 1,
        High,
        Medium,
        Low,
        Unnecesary
    }
    public enum SavingType
    {
        Maximal,
        Regular,
        Minimal
    }
}
